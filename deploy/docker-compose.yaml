services:
  hermes-dee:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.gateway
    image: hermes-gateway:latest
    container_name: hermes-dee
    user: "1000:1004"
    restart: "no"
    volumes:
      - /tank/services/active_services/hermes-dee:/data
      - /run/secrets/hermes-dee/.env:/data/.env:ro
      - /tank/services/active_services/hermes/deploy/config-dee.yaml:/opt/config-seed.yaml:ro
      - /tank/personal/obsidian-personal:/workspace/extra/obsidian
    environment:
      - HERMES_HOME=/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  hermes-tracy:
    image: hermes-gateway:latest
    container_name: hermes-tracy
    restart: "no"
    volumes:
      - /tank/services/active_services/hermes-tracy:/data
      - /run/secrets/hermes-tracy/.env:/data/.env:ro
      - /tank/services/active_services/hermes/deploy/config-tracy.yaml:/opt/config-seed.yaml:ro
      - /tank/personal/tracy-vault:/workspace/extra/obsidian
    environment:
      - HERMES_HOME=/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  hermes-nutrition-bot:
    container_name: hermes-nutrition-bot
    image: hermes-gateway:latest
    user: "1000:1004"
    environment:
      - HERMES_HOME=/data
    volumes:
      - /tank/services/active_services/hermes-nutrition-bot:/data
      - /tank/services/active_services/hermes/deploy/config-nutrition.yaml:/opt/config-seed.yaml:ro
      - /run/secrets/hermes-nutrition-bot/.env:/data/.env:ro
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
